@*Model*@
@model IEnumerable<CinemaModel>
@*Define ViewData*@
@{
	ViewData["Title"] = "List of Cinemas";
}

@*Code*@
<h1 class="studio-header">Studios</h1>

<div class="row">
	<div class="col-md-8 offset-md-2">
		<table class="table">
			<thead>
				<tr class="text-center">
					<th>@Html.DisplayNameFor(model => model.Logo)</th>
					<th>@Html.DisplayNameFor(model => model.Name)</th>
					<th>@Html.DisplayNameFor(model => model.Description)</th>
					<th>Actions</th>
				</tr>
			</thead>
			<tbody>
				@foreach (var item in Model)
				{
				<tr>
					<td class="alert-middle">
						<img class="rounded-end" src="@item.Logo" alt="@item.Name" style="max-width: 150px"/>
					</td>
					<td class="alert-middle">
						@Html.DisplayFor(modelItem => item.Name)
					</td>
					<td class="alert-middle">
						@Html.DisplayFor(modelItem => item.Description)
					</td>
					<td class="alert-middle">
						<a class="btn btn-Outline-primary text-white"><i class="bi bi-pencil-square">Edit</i></a> |
						<a class="btn btn-Outline-info text-white"><i class="bi bi-eye">Details</i></a> |
						<a class="btn btn-danger text-white"><i class="bi bi-trash">Delete</i></a>
					</td>
				</tr>
				}
			</tbody>
			<tr>
				<td></td>
			</tr>
		</table>
	</div>
</div>